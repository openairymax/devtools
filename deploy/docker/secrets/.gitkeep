# Docker Swarm secrets directory
# Place secret files here for Docker Swarm deployment:
#   jwt_secret.txt
#   postgres_password.txt
#   etc.
# These files should NOT be committed to git.
# For docker-compose (non-Swarm), use .env file instead.
